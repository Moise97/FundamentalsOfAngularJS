# FundamentalsOfAngularJS

## Start the Application
  In order to run the application open a terminal and navigate inside the 
  FundamentalsOfAngularJS folder and run the following commands:
  
  ```
  npm install
  node server.js
  ```
